# Kinldy execute the kubectl commands using the centos user.

                sudo su centos
                
# Kubectl commands:
         kubectl get nodes
         kubectl get pods --all-namespaces
