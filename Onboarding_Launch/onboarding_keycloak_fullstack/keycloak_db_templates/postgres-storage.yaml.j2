kind: PersistentVolume
apiVersion: v1
metadata:
  name: {{ clientname }}-keycloak-postgres-pv-volume
  labels:
    type: local
    app: keycloak-postgres
spec:
  storageClassName: manual
  capacity:
    storage: 2Gi
  accessModes:
    - ReadWriteOnce
  hostPath:
    path: "/{{ clientname }}-keycloak/data"

---
kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: {{ clientname }}-keycloak-postgres-pv-claim
  labels:
    app: keycloak-postgres
spec:
  storageClassName: manual
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 2Gi
