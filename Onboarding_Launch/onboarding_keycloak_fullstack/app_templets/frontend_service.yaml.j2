apiVersion: v1
kind: Service
metadata:
  name: frontend
spec:
  ports:
    - port: 3000
      protocol: TCP
      targetPort: 3000
      nodePort: {{ frontendport }}
  type: NodePort
  selector:
    app: frontend
