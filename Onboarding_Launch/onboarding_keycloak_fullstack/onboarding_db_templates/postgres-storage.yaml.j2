kind: PersistentVolume
apiVersion: v1
metadata:
  name: {{ clientname }}-onboarding-postgres-pv-volume
  labels:
    type: local
    app: onboarding-postgres
spec:
  storageClassName: manual
  capacity:
    storage: 2Gi
  accessModes:
    - ReadWriteOnce
  hostPath:
    path: "/{{ clientname }}-onboarding/data"

---
kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: {{ clientname }}-onboarding-postgres-pv-claim
  labels:
    app: onboarding-postgres
spec:
  storageClassName: manual
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 2Gi
