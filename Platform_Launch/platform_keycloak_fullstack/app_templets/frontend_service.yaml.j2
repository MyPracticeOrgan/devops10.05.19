apiVersion: v1
kind: Service
metadata:
  name: frontend
spec:
  ports:
    - port: 3001
      protocol: TCP
      targetPort: 3001
      nodePort: {{ frontendport }}
  type: NodePort
  selector:
    app: frontend
